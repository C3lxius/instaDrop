import 'package:flutter/material.dart';

class SearchStackered extends StatelessWidget {
  const SearchStackered({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
